<?xml version="1.0" encoding="utf-8"?>
<!--
  __PROJECT_NAME__ — apps/android sample manifest.

  Permissions the :src library declares merge in from its manifest automatically.
  Add app-only permissions here. The minimal sample needs none.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="__DISPLAY_NAME__"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
