---
title: Example
---

# Example

TODO: replace this with a real, task-focused recipe. A recipe answers one
"how do I…?" question with a short, copy-pasteable snippet and just enough prose
to explain the moving parts.

```kotlin
import com.happycodelucky.src.Greeter

// A minimal end-to-end example. Swap `Greeter` for your real API.
val message = Greeter().greet()
println(message)
```

Each recipe page must contain at least one fenced code block — `docs/check.py`
enforces this for everything under `recipes/`.
