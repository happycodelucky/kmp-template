---
title: macOS
---

# macOS

TODO: document anything macOS consumers need beyond adding the package.

If the host app is sandboxed (Mac App Store), list the App Sandbox entitlements
the library requires — for example outgoing/incoming network connections, or the
local network entitlement for discovery. Note the minimum deployment target.
